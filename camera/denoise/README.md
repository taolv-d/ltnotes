# Denoise

这里收录以噪声为核心变量的笔记，包括噪声建模、监督/自监督去噪、Noise2Noise/R2R/GR2R、RAW/RGB 去噪方法。

放这里的判断：文章主要讨论 `noise`、`sigma`、`ISO`、shot noise、read noise、成对/非成对噪声样本，或者目标是去噪质量。

不放这里的情况：如果重点是曝光不足、可见性、夜间检测或 Retinex 增强，放到 `camera/low_light`；如果重点是 U-Net、attention、loss 这类通用方法，放到 `machine learning` 后再从这里链接。