---
type: note
status: done
tags:
  - isp
  - awb
rating: 0
create: 2026-05-24
update: 2026-09-05
---
# AWB
camera 需要 awb 的主要原因是：让相机拍出来的结果跟人眼睛看到的一致：
1. 相机对RGB三种色光的光谱响应与人眼有差异
2. 人眼色彩恒常性，光源色温变化时，人眼看到的白色仍然是白色。但相机看到的就不一样了。

上面两点反应到 raw 图上就是R/G B/G的差异。AWB 通过让场景中灰色物体仍然为灰色（R=G=B）来对齐到人眼，具体做法就是就是给 RGB 三个通道乘一个增益（其中G通常为1）。

因此 AWB 可以分为两个部分：
1. 找到图像中的灰色——AWB 算法
2. 根据灰色区域估计色温，并找到对应的 Rgain Bgain 补偿——AWB 标定确定
# 基础知识
## 色温

色温（Colour Temperature）是表示光源光色的尺度，其单位为**开尔文（K）**。理论上，色温基于**绝对黑体辐射原理**：当黑体从绝对零度（-273℃）加热时，会依次发出红、橙、黄、白、蓝色光。光源的色温即为其光谱与某一温度下黑体辐射光谱相匹配时的温度值。
![[attachments/Pasted image 20260905183052.png]]

下图是CIE1931 中 色温Planck轨迹^[https://www.opticskit.cn/tools/chromaticity]
图中绿色三角为sRGB色域
![[attachments/Pasted image 20260905183414.png|532]]

**光源色温对照表**

| 光源名称/代号 | 色温 (K) | 典型应用场景/说明                          |
| ------- | ------ | ---------------------------------- |
| H       | 2300   |                                    |
| A       | 2856   | 模拟白炽灯（钨丝灯）照明                       |
| F       | 2700   | 模拟家庭或酒店常见的暖白荧光灯                    |
| D50     | 5000   | **ISP 标定常用**                       |
| D65     | 6500   | 模拟国际标准的人工日光，ISP 常用                 |
| D75     | 7500   | 晴天天空                               |
| TL84    | 4000   | 模拟欧洲、日本、中国等地商店常用的照明光源。             |
| CWF     | 4150   | 模拟美国冷白商店光源（Cool White Fluorescent） |
| TL83    | 约3000  | 模拟欧洲标准的暖白商店光源（Warm White）          |
| U30     | 3000   | 模拟美国暖白商店光源                         |

# AWB 标定
AWB 标定通常包含两种含义：
1. 同一相机模组的不同个体之间存在R/G B/G的差异（批次，sensor、镜头、IR、镀膜等差异）。通过标定将他们都矫正到平均值。
2. 对相机模组，标定它在不同色温下（拍摄的灰色区域）R/G B/G值。
这里主要介绍第二点。（第一个在[[../ISP 标定|ISP 标定]]中简要介绍）

以下描述来自一个芯片手册中的AWB标定步^[https://doc.sophgo.com/cvitek-develop-docs/master/docs_latest_release/CV180x_CV181x/zh/01.software/ISP/ISP_Tuning_Guide/build/html/5_Module_Function.html#white-balance]
>**步骤1.** 在照度 600 lux 的光源下（左右两测的光源需均匀， 光源与色卡的夹角在 25°- 45°），在光源类型的选择上，请尽量满足高，中、低色温至少各有一组。推荐使用D65（6500K）或D75（7500K ）、TL84（4000K ）和 A （2800K ）三组光源。
>
>**步骤2.** 采集Raw时请尽量使灰卡画面占据70%以上的画面内容，并确认 Raw 的亮度符合预期，G分量亮度在饱和值的0.36倍左右（若是12 bits raw，建议G值在 1274 ~ 1674 ），采集帧数1帧即可，采集时需要记录实际环境色温。
>
>**步骤3.** 由于镜头Shading 会影响 AWB 的标定结果，为确保 AWB 标定结果准确，请将采集下来的Raw进行Shading标定后再进行AWB标定。

上述操作简单来说就是标定不同色温下的 **R/G B/G**，从而得到Plankian 曲线，如下图所示：
![[attachments/Pasted image 20260519164349.png]]
标定坐标系不一定是 R/G B/G，也可以在 CrCb、XYZ 等坐标系下标定。也可以多种坐标系下同时标定，互相印证排除异常值（想象下三维空间中的点向二维空间投影，不同的投影方式得到的结果分布不一样。在一个投影中误判的点，在其他投影方式里可以很好的分开）。

**YCrCb**
下图中 Cb Cr 都是负数，对应到YCrCb空间应该是偏绿色的部分，对应到sensor raw图直接转rgb时灰色部分的表现（图中点对应 color checker 中不同亮度的灰块）。
![[attachments/Pasted image 20260519165006.png|557]]

**XY**
下图时RK ISP 标定的XY域图像。这里XY域具体变换不清楚怎么做的，但是猜测跟CIE1931中的XYZ变换有关系。
![[attachments/Pasted image 20260519170051.png|493]]

 
# AWB 算法
AWB 算法就是找到图像中灰色的部分，根据实际灰色表现（R/G B/G），对应到标定值（需要插值、考虑多光光源等）得到矫正gain。
常见的几种 AWB 算法如下表所示：

| 缩写/简称 | 算法名称                | 核心思路                        |
| ----- | ------------------- | --------------------------- |
| GW    | 灰度世界法               | 假设场景中RGB三通道平均值趋近于灰色         |
| PRM   | 完美反射法               | 假设图像中最亮的点就是白点               |
| WPR   | White Patch Retinex | 假设场景中存在白色块，反射所有波段的光         |
| MWP   | 改进型White Patch      | 在L* a * b *空间改进WPR的不足       |
| WGE   | 加权灰度边缘法             | 根据不同边缘类型（材质、阴影等）赋予不同权重来估计光源 |
| LSR   | 局部表面反射统计法           | 基于局部表面反射统计进行光源估计            |
| DH    | 动态阈值法               | YUV域动态检测近白区域                |

以上这些算法基本来源于以下这些假设：
1. 一幅图像的平均反射颜色是灰色的，该理论可以追溯到早期色彩恒常性。代表方法就是灰度世界法。他的局限性也很明显，接近纯色画面（例如长焦镜头）容易失效
2. 图像中最亮的地方是白点，也是比较早期的理论了，完美反射法、白块法都是基于这个理论。如果最亮的地方不是白点，这个方法也寄了
3. 图像中物体边缘的阴影是灰色的，这个方法是2007年提出的，不过算法受边缘分类准确性的影响，如果误识别颜色边缘可能会影响结果^[http://colorconstancy.com/source-code/#bayesian]
4. LSR 方法没有一个公认的起源，因为他的局部统计思想很多地方都有用
5. DH 方法是2005 提出的算法，他是在YUV域计算，假设UV域中中心区域是灰色的，思想类似灰度世界法。

关于这些算法的对比，一片医学方向的论文（可能工程价值不高）做了一些对比，可以看看他的对比实验来了解这些算法的性能^[A Novel Automatic White Balance Method for  Color Constancy Under Different  Color Temperatures https://ieeexplore.ieee.org/document/9509540]
## 传统AWB 算法
### Grey World
Grey World 假设一幅自然图像的平均反射颜色应为灰色，即：
$$
R_{mean} \approx G_{mean} \approx B_{mean}
$$
各个通道的增益：
$$
\begin{split}
R_{gain}&=G_{mean}/R_{mean}\\
G_{gain}&=1\\
B_{gain}&=G_{mean}/B_{mean}
\end{split}
$$
即：
$$
\begin{split}
C_{gain}&=K/C_{mean}
\end{split}
$$
其中 $K=(R_{mean}+G_{mean}+B_{mean})/3$, 是全图整体灰度均值。
灰度世界通常作为白点检测失效时的备用辅助。
### White Patch
White Patch 假设画面中最亮区域接近白色。找到图像中最亮（前 T%）的白点，或者白块，统计它们的均值得到$R_{white},G_{white},B_{white}$，进而得到各个通道的增益
$$
\begin{split}
C_{gain}&=K/C_{white}
\end{split}
$$
其中：
1. $K=G_{white}$ 时$G_{gain}=1$, G 通道信噪比高，因此更常用
2. $K=(R_{mean}+G_{mean}+B_{mean})/3$，此时跟灰度世界法第二种情况类似
### 局部表面反射统计法
该方法与白点法/白块法非常相似，但他不是找最亮的地方，而是找最灰的部分：$R\approx G \approx B$
显然他的假设有些站不住脚，但统计上大部分场景确实也有效果
### Shades of Grey
Shades of Grey 是 Grey World 和 White Patch 的统一形式，使用 Minkowski 范数：
$$
L_p(C) = {(1/N * \sum_i {C_i}^p)}^{(1/p)}
$$
当 `p=1` 时接近 Grey World；当 `p` 趋近无穷大时接近 White Patch。
增益：
$$
C_{gain} = K / L_p(C)
$$
灰边法的难点在于如何区分各种不同边缘，找到边缘对图像求导就行。边缘有以下几类：
1. **材质边缘**：不同物体材质之间的交界处，通常**颜色变化，但亮度不变**
2. **阴影边缘**：光照变化造成的明暗交界，**亮度变换，颜色不变**
3. **高光边缘**：镜面反射高光区域的边缘，**亮度、颜色突变，发白**
这三类中 2 3 携带了更多光源信息，因此应该赋予高权重。1 反应的是颜色变化，因此权重较低
### 动态阈值法 DH
先看动态阈值法的算法步骤：
1. 动态阈值法先将图像转到YUV空间，只关注UV平面，这样可以避免亮度影响。
2. 计算色度中心：$U_{mean}, V_{mean}$, 统计色度分散程度 $U_{std}, V_{std}$，描述色彩是聚成一团，还是分散的，以及区域中心
3. 计算动态矩形框，落在矩形框内的点参与白平衡计算：
	- 中心：$(U_{mean}, V_{mean})$
	- 大小： $T*U_{std}, T*V_{std}$，其中 $T$ 为固定阈值
4. 找到矩形框内的点，计算它们的RGB值，进一步计算 AWB 增益

显然，极端色温时，本方法也会失效。
同时该方法跟灰度世界法思想上有些类似
### IctWB
这是 rawtherapee 软件中的 AWB 算法，严格来说跟这里的讨论有区别，不过值得一看[[IctWB]]
## 现代 AWB 方法

现代AWB方法显然不是上面介绍的那些简单方法。它们都有各自的理论漏洞，且比较容易触发。
现代AWB 做法通常如下：
1. 多种传统方法组合，取长补短。不过本人实测过一些SoC的 AWB 算法，确实是多种算法混合，但效果仍然不行，一个原因就是这些传统算法的短板往往是一致的，并没有补短
2. 异常场景标定（黑名单），例如蓝天、绿地等纯色场景，提前标定好，AWB 找到的白点跟这些标定一致，那就说明进入了纯色场景。动态阈值法往往会搭配异常点剔除。很多SoC 的 tunning 工具就支持标定异常白点（例如偏离普朗克曲线）。
3. 引入色温传感器
4. NN 方法，看起来也是AWB 发展的方向了，在这里展开[[NN AWB]]

## 工程化注意点

- 饱和像素剔除，超过阈值的像素不放入AWB统计中，避免高光clip
-  AWB 统计最好在 LSC 后进行，否则暗角和色彩阴影会污染平均值。
- 大面积单色场景会违反 Grey World 假设，例如草地、蓝天、红墙。
- 高亮区域不一定是白色，White Patch 在舞台灯、彩灯场景容易失效。
- AWB 输出增益应做限幅和平滑，否则视频中会出现色温跳变。
- 多光源场景，估计多个光源的概率，矫正参数混合

此外，SoC 厂商提供的AWB 算法一般比较基础。效果比较好的AWB 往往需要标定更多色温/场景（阴影、树林等），同时也会不同亮度下标定，甚至区分室内室外。