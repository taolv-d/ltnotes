# Low Light

这里收录以低照度和可见性为核心问题的笔记，包括低光增强、Retinex、夜间检测、低光视觉综述、低光下的感知任务。

放这里的判断：文章主要讨论 `illumination`、`exposure`、visibility、night、low-light recognition，或者增强结果直接服务于低光视觉任务。

和 `denoise` 的边界：低光常伴随噪声，但如果文章重点是 RAW 噪声模型或去噪训练范式，优先放 `camera/denoise` 或 `camera/raw_denoise`；如果重点是亮度、照明、识别任务，放这里。