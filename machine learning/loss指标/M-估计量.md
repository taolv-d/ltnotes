---
type: note
status: draft
tags:
  - machine-learning
  - loss
rating: 0
create: 2026-04-22
update:
---
**M-估计量（M-estimator）** 是“最大似然型估计量”（Maximum-likelihood-like estimator）的缩写，由统计学家 Peter Huber 在 1964 年正式提出。

任何通过最小化$\sum\rho(y_i-\hat{y}_i)$  或 $E[\rho(r)]$ 来估计参数的损失函数，都属于 **M-估计量** 的范畴。
其中$\rho(r)$是一个关于残差$r$的函数。

对于L1 L2 等损失函数，都是$\rho(r)$ **的不同选择**。

```
M-估计量（M-estimator）
│
├── 经典 Lp 范数
│   ├── L2（最小二乘）
│   ├── L1（最小绝对偏差）
│   └── L0（理论上限）
│
├── 稳健 M-估计
│   ├── Huber 损失
│   ├── Smooth L1（≈ Huber 特例）
│   ├── Tukey 双权损失
│   └── Cauchy 损失
│
└── 退火/近似 L0
    ├── 退火 L0（Noise2Noise）
    ├── Geman-McClure
    └── Welsch 损失
```

# 经典Lp范数

假设有一张图像，对于任意像素位置 $i$，$y_i$ 代表该位置的真实像素值（Ground Truth），$\hat{y}_i$ 代表模型预测的像素值，$N$ 代表像素总数。

| 损失名称        | 数学公式                                                 | 别称                                | 异常值（离群点）敏感性                          | 收敛                                                           | 梯度公式<br>（反向传播）                                                                              |
| ----------- | ---------------------------------------------------- | --------------------------------- | ------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **L1 Loss** | $$ L_1 = \frac{1}{N} \sum_{i=1}^{N}(y_i-\hat{y}_i)$$ | **MAE** <br>(Mean Absolute Error) | 不敏感<br>梯度绝对值始终为 $1$（或 $-1$），模型更新步长稳定 | 梯度在零点不连续。解趋向于**稀疏**，有利于保留图像中的**边缘**和**高频细节**，常用于超分辨率任务。      | $\frac{\partial L_1}{\partial \hat{y}_i} = \text{sign}(\hat{y}_i - y_i)$<br><br>sign 是取符号函数 |
| **L2 Loss** | $$L2=\frac{1}{N}\sum_{i=1}^N(y_i−\hat{y_i})^2$$      | **MSE** <br>(Mean Squared Error)  | 极其敏感<br>大误差会主导loss                   | 梯度平滑连续。解是唯一的且趋向于平均值。**致命缺点**是生成的图像往往**模糊**，因为它倾向于对所有可能的解求平均。 | $\frac{\partial L_2}{\partial \hat{y}_i} = 2(\hat{y}_i - y_i)$                              |

| 损失函数        | 对误差（残差）的处理方式                                             | 图像效果                                                          |
| ----------- | -------------------------------------------------------- | ------------------------------------------------------------- |
| **L1 Loss** | 极小化误差的**绝对值**。它允许少数像素存在**很大的误差**（边缘处），只要绝大多数像素误差为 **0**。 | 既然边缘处误差大一点代价可控，模型就不会为了迁就边缘的像素差而去改动平坦区域的像素。**视觉表现：边缘锐利、背景干净。** |
| **L2 Loss** | 极小化误差的**能量**（平方和）。它倾向于把大误差拆分成很多个小误差。                     | 把边缘的剧烈落差"摊平"到周围的平坦区域。**视觉表现：边缘模糊、振铃效应。**                      |

**L1 L2 loss 的分工**

| 场景                  | 推荐 Loss       | 原因                          |
| ------------------- | ------------- | --------------------------- |
| **图像分类、逻辑回归**       | 交叉熵           | 概率分布匹配                      |
| **目标检测框回归**         | **Smooth L1** | 结合 L2 的平滑收敛 和 L1 的抗离群       |
| **图像超分/去噪（追求像素精度）** | **L2 (MSE)**  | 峰值信噪比（PSNR）指标与 L2 直接挂钩；平滑收敛 |
| **图像超分/去噪（追求视觉锐利）** | L1 + 感知Loss   | 利用 L1 的稀疏性保留边缘              |
| **传统数值分析、凸优化求解**    | **L2**        | **唯一有解析解，计算最快**             |

## L1 loss -> 中值

对于一维标量 $y$，我们想找到一个点 $c$ 最小化期望 $L1$​ 损失：

$$
\begin{split}
L(c)
&=E_{y\sim p(y)}[|c-y|]\\
&=\int_{-\infty}^{\infty}|c-y|p(y)dy
\end{split}
$$
为了找到最优的$c$，我们对$c$求导并令导数为零。

**关键技巧**：
$$
\frac{d}{dc}|c-y|=sign(c-y)
$$
除了$c=y$处不可导。

导数：
$$
L(c)^\prime=\int_{-\infty}^\infty sign(c-y)p(y)dy
$$

令导数为0，分解sign函数后：
$$
L(c)^\prime=\int_{-\infty}^c(+1)p(y)dy+\int_{c}^\infty(-1)p(y)d(y)=0
$$

$$
P(y\leq c)-P(y>c)=0
$$
$$
P(y\leq c)=P(y>c)
$$

这意味着$c$将概率质量分成相等的两半。
$$
P(y\leq c)=\frac{1}{2}
$$
这正是**中位数**的定义。


## L2 Loss → 均值

对于一个随机变量 $y\sim p(y)$，我们要找一个点 $c$ 最小化 **L2 损失**的期望：
$$
\begin{split}
L(c)
&=E_y[(c-y)^2]\\
&=E[c^2-2cy+y^2]\\
&=c^2-2cE(y)+E(y^2)

\end{split}
$$
对 $y$ 求导并令为 0：
$$
2c-2E[y]=0
$$
即$c=E[y]$,即均值

## L2 loss 的特殊性

**L2 loss 是一个数学性质最"好"、最稳定、最容易优化的凸函数。**

**唯一具有"封闭式解析解"的损失函数**

在经典的线性回归问题中，如果使用 L2 Loss（最小二乘法），我们可以直接通过一个数学公式算出最优解（仅L2 能这样计算，L1不行）：  
$$\hat{β}=(X^TX)^{−1}X^Ty$$
这意味着在很多**非深度学习的传统统计模型、信号处理、控制系统**中，L2 是唯一能给出**确定性、唯一解**的选择。

**唯一处处"平滑可导"的损失函数**

L2 Loss 的导数是 $2x$，这是一个**连续且线性变化**的函数。

- **L2 梯度**：误差越小，梯度越小 $\rightarrow$ 模型步长自动变小 $\rightarrow$ **更容易精确收敛到极小值点**
- **L1 梯度**：导数永远是 $\pm 1$。即使在极小值点附近，梯度依然很大 $\rightarrow$ 模型在最优解附近**反复横跳**，很难稳定下来。

**这就是著名的"L1 收敛不稳定"问题。** 在深度学习中，优化器（如 Adam、SGD）最喜欢的就是 L2 这种**平滑、有良好 Lipschitz 连续性的函数**。这也是为什么很多时候即便最后评价指标是 L1，训练初期或者辅助分支也会挂一个 L2 Loss。

**高斯噪声假设下的"最大似然估计"** [[../../TODO|TODO]]

- **L2 Loss** 等价于假设数据中的噪声服从**正态分布（高斯分布）**。
- **L1 Loss** 等价于假设数据中的噪声服从**拉普拉斯分布**。

自然界和物理传感器（如相机 CMOS）产生的**随机噪声**，绝大多数都完美符合**正态分布**。因此，如果你想让模型从带有随机高斯噪声的图像中恢复出原始信号，**数学上最优的估计器就是最小化 L2 Loss**。


## L0 Loss

L0 “范数”：
$$
||z||_0=
\begin{cases}
	\begin{split}
	0 \quad z=0\\
	1 \quad z\neq0
	\end{split}
\end{cases}
$$

对应的损失：
$$
L_0(z,y)=||z-y||_0
$$
也就是：

- 预测完全正确 → 损失 0
- 有任何误差 → 损失 1

显然：

- 不可导
- 梯度几乎处处为 0
- 无法用 SGD 优化



# 退火、近似L0
## 退火 L0 Loss → 众数

### 问题背景

在 **随机脉冲噪声** 场景中：

- 一个像素以概率 $p$ 被替换为随机颜色
- 以概率 $1-p$ 保留原始颜色

分布是：

- 一个 **Dirac spike**（原始颜色）
- 一个 **均匀分布**（随机颜色）

此时：

- 均值 ❌ 不对 (会被拉向随机颜色均值）
- 中位数 ❓ 不一定对（如果 $p<0.5$，中位数 = 原始颜色 ✅；但如果 $p>0.5$，中位数 = 随机颜色 ❌）
- **众数 = 原始颜色**（只要 $p<1$，原因是被替换的是随机颜色，随机颜色显然很难全部都一样，因此众数依然是未被替换的颜色）

问题变成：**如何让网络逼近众数？**

###  退火近似：从 L2 到 L0

[[../../camera/denoise/2018 Noise2Noise NVIDIA|2018 Noise2Noise]]使用了一个 **退火 L0 近似**：

$$
L_{annealed}(z,y)=(|z-y|+\varepsilon)^\gamma
$$
其中：

- $\varepsilon$ 是一个极小常数（$10^{-8}$）
- $\gamma$ 在训练过程中 **从 2 线性退火到 0**

### 退火过程的行为

| $\gamma$               | 近似损失  | 最优解 |
| ---------------------- | ----- | --- |
| $\gamma =2$            | 近似 L2 | 均值  |
| $\gamma =1$            | 近似 L1 | 中位数 |
| $\gamma \rightarrow 0$ | 近似 L0 | 众数  |

当$\gamma \rightarrow 0$ 时：
$$
(|d|+\varepsilon)^\gamma\rightarrow
\begin{cases}
	\begin{split}
		1 \quad |d|>0 \\
		0 \quad d=0
	\end{split}
\end{cases}
$$

也就是说：**只要预测 ≠ 目标，损失 ≈ 1**。(非零的零次方为1，零的次方都是零)

此时，网络为了让损失最小，会尽量选择 **最常见的目标值**，即 **众数**。

# 稳健 M 估计
## Smooth L1 Loss

由于 L1 在 0 点处不可导（震荡）、L2 容易梯度爆炸（对离群点），在目标检测的边界框回归中常用 **Smooth L1 Loss**：

$$Smooth L1(x)=
\begin{cases}
0.5x^2, & |x|<0.5 \\
|x|-0.5, & \text{otherwise}
\end{cases}$$

它在误差较小时是 L2 的平滑特性（容易收敛到精确值），在误差较大时退化为 L1 的特性（避免梯度爆炸）。

这个 0.5 确实不是随便取的，它是为了让 **Smooth L1 Loss** 在分段点 $x=1$ 处**不仅函数值连续，而且一阶导数也连续**。(这个推导很简单，将分界点设为未知数就好)

这就保证了在反向传播时，梯度是平滑过渡的，不会发生突变。

|Loss 类型|梯度公式 (绝对值)|梯度特性|
|---|---|---|
|**L2 Loss**|$\|2x\|$|误差越大，梯度越大，易**梯度爆炸**|
|**L1 Loss**|$1$|梯度永远是常数，零点处**不可导**|
|**Smooth L1**|$\|x\| \text{ (if } \|x\|<1\text{)} \text{ or } 1$|**零点平滑 + 远处截断**|

