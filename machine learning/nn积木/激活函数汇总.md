
## ReLU
`ReLU(x) = max(0, x)`

- 大于 0，原样通过
- 小于 0，直接变 0

图像上看就是左边全砍掉，右边保留直线。

它的优点：

- 简单
- 快
- 很经典

它的问题：

- 负数部分全部变 0，信息直接没了
- 有些神经元可能长期输出 0，出现“死 ReLU”

## SiLU (Swish)

`SiLU(x) = x * sigmoid(x)`

它不是一刀切，而是更平滑：

- 很大的正数，差不多原样通过
- 很小的负数，不是直接砍成 0，而是保留一点点
- 过渡很平滑